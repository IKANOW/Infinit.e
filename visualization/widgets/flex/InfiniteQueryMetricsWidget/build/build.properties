# Project Metadata
application.name = Infinit.e
src.application.name = ${ant.project.name}.mxml
package.root = com/ikanow/infinit/e

# Flex sdk
flex = /Applications/Adobe Flash Builder 4.5/sdks
flex.sdk.version = 4.5.1.21328
flex.sdk = ${flex}/${flex.sdk.version}
#flex.sdk = c:/dev/tools/sdks/flex/4.5.1.21328

# Distribution
dist.loc = ${basedir}/dist
dist.swf.name = InfiniteQueryMetricsWidget
#(^^ for bw compatiblity with the old Google Maps)

# Build location
build.loc = ${basedir}/build

# Libraries used by build
build.libs.loc = ${build.loc}/flex

# Output locations
build.output.loc = ${build.loc}/output
bin.loc = ${build.output.loc}/bin

# Project source / library locations
src.loc = ${basedir}/src
libs.loc = ${basedir}/libs
javascript.loc=${basedir}/html-template